// IoT Core rules should react to the connection status topic and invoke this lambda to
// update shadow device, and put status in dynamodb for history

export const handler = async (event: any) => {};
